@model Bookmaker.Models.Voyage

@{
    ViewBag.Title = "Modifier un voyage";
}

<h2 class="crud">@Html.ActionCrud("Voyages")</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <fieldset class="sixteen columns">
        <legend>@Model.Title</legend>

        @Html.HiddenFor(model => model.VoyageID)
        @Html.Partial("_Editor", Model)

        <p class="offset-by-five eleven columns alpha">
            <input type="submit" value="Enregistrer" />
            @Html.ActionCancel()
        </p>
    </fieldset>
}

@section JavaScript
{
    @Html.Partial("_FormScripts")
}