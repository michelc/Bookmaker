@model IEnumerable<Bookmaker.Models.BookletIndex>

@{
    ViewBag.Title = "Toutes les brochures";
}

<h2 class="crud">@Html.ActionCrud("Brochures")</h2>

<table>
<thead>
    <tr>
        <th></th>
        <th>
            Année
        </th>
        <th>
            Titre
        </th>
        <th></th>
        <th>
            Journées
        </th>
        <th>
            Séjours
        </th>
        <th></th>
    </tr>
</thead>
<tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Booklet_ID }, new { @class = "edit" })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.ActionLink(@item.Title, "Details", new { id = item.Booklet_ID })
        </td>
        <td>
            @Html.ActionLink("Generate", "Generate", new { id = item.Booklet_ID }, new { @class = "icon doc_file" })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TravelsCount1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TravelsCount2)
        </td>
        <td>
            @Html.ActionLink("Delete", "Delete", new { id = item.Booklet_ID }, new { @class = "delete" })
        </td>
    </tr>
}
</tbody>
</table>
